package ordernacao;

import java.util.List;

public class Merge {

    public List<Integer> arrumar(int x, int y, List<Integer> lista){
        int qtd = lista.size();
        if(qtd == 2){
            if()
        }
    }

    public List<Integer> mergeSort(List<Integer> merge){
        int inicio = 0;
        int fim = merge.size() - 1;
        int meio = Math.abs(merge.size() / 2);

        List<Integer> direita = arrumar(inicio,meio,merge);
        List<Integer> esquerda = arrumar(meio,fim,merge);

    }
}
